using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Prey : MonoBehaviour
{
    Vector3 size;
    float mess;
    float vision_depth, vision_angle, idleVelocity, escapeVelocity, topAccelerity;
    float energy;
    Vector3 current_position, current_velocity, current_orientation;
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
